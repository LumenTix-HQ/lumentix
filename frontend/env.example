# Stellar Network Configuration
# Options: testnet, mainnet
# Note: Wallet connection will validate against this network
NEXT_PUBLIC_STELLAR_NETWORK=testnet
NEXT_PUBLIC_HORIZON_URL=https://horizon-testnet.stellar.org

# For mainnet, use:
# NEXT_PUBLIC_STELLAR_NETWORK=mainnet
# NEXT_PUBLIC_HORIZON_URL=https://horizon.stellar.org

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000

# Platform Account (for escrow and operations)
NEXT_PUBLIC_PLATFORM_ACCOUNT=YOUR_PLATFORM_ACCOUNT_PUBLIC_KEY

# WalletConnect project id (required to enable the WalletConnect connector).
# Obtain one at https://cloud.walletconnect.com. Leave unset to disable WalletConnect.
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=
